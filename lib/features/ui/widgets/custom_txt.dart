import 'package:flutter/material.dart';

class CustomTxt extends StatelessWidget {
  const CustomTxt({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
